# The security headers every response carries, whichever Content-Security-Policy goes with it.
# Included only by security-headers.conf and order-security-headers.conf, never on its own.
add_header X-Content-Type-Options nosniff always;
add_header X-Frame-Options SAMEORIGIN always;
add_header Referrer-Policy strict-origin-when-cross-origin always;
