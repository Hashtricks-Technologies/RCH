apiVersion: v1
kind: Service
metadata: { name: {{ .Release.Name }}-ui, labels: { {{- include "rch.labels" . | nindent 4 }} } }
spec: { selector: { app.kubernetes.io/instance: {{ .Release.Name }}, app.kubernetes.io/component: ui }, ports: [{ name: http, port: 8080, targetPort: http }] }
