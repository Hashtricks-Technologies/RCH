{{- if .Values.secrets.create }}
apiVersion: v1
kind: Secret
metadata: { name: {{ include "rch.secretName" . }} }
type: Opaque
stringData:
{{- range $k, $v := .Values.secrets.values }}
  {{ $k }}: {{ $v | quote }}
{{- end }}
{{- end }}
