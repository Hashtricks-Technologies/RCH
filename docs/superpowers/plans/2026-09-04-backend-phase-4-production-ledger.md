# SDD ledger — plan: docs/superpowers/plans/2026-09-04-backend-phase-4-production.md
Setup: Phase 3 fast-forwarded into develop at b086052 and pushed (CI run 33835885660 in progress). Branch feat/phase-4-production cut from develop; plan committed at 81d2c24 (7 tasks, 4 waves). Pre-flight scan (opus) → preflight-scan.md: 36 rows, 0 same-wave file conflicts, 2 Blocking / 7 Should-fix / 9 Notes.
Rulings: F1 split the transitions test so Task 1 ends green; F2 no REQUEST_TRANSITIONS edge from "Ticket issued" back to the approved statuses — Task 5's cancel guards the request status explicitly and writes the release itself (a cancelled ticket's request may be re-issued); F3 the payers migration is merged, so Task 1's enum migration is 0005 (generated), and every worktree forks from the phase branch head after the journal is checked; F4–F9 applied as the scan proposed (imports the compiler names; lock the finished cell only when made > 0; warmPool already imported; snapshot.test seeds in beforeAll; store TicketDrawer stepper + prod Tickets SHOW filter handle Cancelled). Planner amending the plan now; wave 1 (T1 ∥ T2) dispatches from the amended commit.
Plan amended and committed at ab39c56. Journal last idx 4 (0004_payers) — Task 1's migration is 0005. Wave 1 dispatched: T1 contract + ticket_status enum + transitions (opus) ∥ T2 shelf-life domain + UI bestBefore (sonnet), worktrees, BASE ab39c56.
CI on develop GREEN for the Phase 3 merge (run 33835885660: check 4m51s, chart, images incl. kind install 3m55s).
Task 2: implementer DONE — fd9c340 (worktree-agent-a1affb532eb39f988); domain 47, ui 340. Review dispatched (sonnet).
Task 1: implementer DONE_WITH_CONCERNS — c72faa6 (worktree-agent-ab85debe5e859679b); contract 34, domain 47, api 327; migration 0005_ticket_status_cancelled. Ruling: the one compile-fixing line in store/TicketDrawer.tsx (STEPS.indexOf cast, plan L2032) is accepted out of scope — a widened union must not leave the branch red for wave 2; Task 6 skips that line. Review dispatched (sonnet).
Task 2: review — Approved (note: domain CLAUDE.md's 'one platform API, in ids.ts' line is now stale — Task 7 refreshes the guides). Merged --no-ff as 5fea59e.
Task 2: complete
Task 1: review — Approved (no issues). Merged --no-ff as f7e507e.
Task 1: complete
Wave 1 merged; typecheck 4/4; journal last idx 5. Wave 2 dispatched from BASE f7e507e: T3 reads (sonnet) ∥ T4 production status + batches (opus) ∥ T5 ticket cancellation (opus), worktrees.
Task 3: implementer DONE — 5b2c534 (worktree-agent-a687f195ecbcf8595); api 332. Review dispatched (sonnet).
Task 3: review — Approved (no issues). Merged --no-ff as 18c1a25.
Task 3: complete
Task 5: implementer DONE — d3d4bbd (worktree-agent-abf49bd72a77d3b6e); api 337, tickets 31 ×3, race proven. Deviations accepted: canTransition + two sentences instead of assertTransition (the tests pin two sentences); fixture-relative quantities (the brief's 20 L milk exceeded the store's 12 L); location name from the fixture. Review dispatched (opus).
Task 4: implementer DONE — b101481 (worktree-agent-a258d4256015632c7); production 38 ×3, api 349. Deviations accepted: the brief's two-batches race cannot fail (the batch sequence row serialises minting) — kept plus a batch-vs-distribution race that does fail without the locks; chai cases bake tea leaf first (kitchen has none). Review dispatched (opus).
Task 5: review — Approved (minors: the Received refusal sentence says 'on its way' after it landed → §16 note / Task 7; voidTicket's return unused; a second writer of tickets.status; request history row without the cancelled ticket id; ticket→prod_order lock order vs dispatch documented). Merged --no-ff as 9847421.
Task 5: complete
Task 4: review — Approved (minors: overrideAt truthiness on reason; empty-note asymmetry between write result and reader; shortage sentence built twice; the kept two-makes race lacks its honest comment) → integration tidy before Task 6's merge or Task 7. Merged --no-ff as 9a07758.
Task 4: complete
Wave 2 merged; gate above. Task 6 (UI cutover of the kitchen's screens + ticket cancel button) dispatched — opus, worktree, BASE 9a07758.
Attribution rule changed by the harness: commits end with only 'Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>' (no Claude-Session trailer) from here on; dispatches say so. Production-module tidy (Task 4 minors) dispatched in-tree (sonnet) alongside Task 6.
Phase 5 plan drafting dispatched in parallel (opus) → docs/superpowers/plans/2026-09-04-backend-phase-5-procurement.md (uncommitted until Phase 4 lands on develop).
Production tidy in-tree 0e57155 (Task 4 minors; production 39 tests).
Task 6: implementer DONE — 87a799a (worktree-agent-ada3e9d7539c868b1); ui 343 (+1 todo), api 364; live-checked by curl and browser. Deviations accepted: cancelBusy name; two footer sentences given a Cancelled branch. Note for Task 7: a cancelled ticket leaves the issue desk (lists Issued/Collected). The live check left documents in the shared dev DB (harmless — Task 7 reseeds). Review dispatched (opus).
Task 6: review — Needs fixes: (I1) five display sites read a cancelled ticket as delivered/in transit/open (prod Tickets ×3, Dashboard, MakeDistribute); (I2) canMoveOrder/canCancelTicket untested (the Dispatched-source line is the one non-table rule); minors: canMoveOrder lacks the target guard; store TicketDrawer OTP box gates on a literal; UI/CLAUDE.md line now false (Task 7); cancelled tickets unreachable from store screens (note); make tile busy lock unpinned. Fix round 1 dispatched (resume ada3e9d7539c868b1).
Task 6: fix round 1 — b36616e (five sites + Reports/manager Dashboard/ItemsStock fixed; isTicketOpen selector; canMoveOrder target guard; selector tests; make-tile lock test proven). Scoped re-review dispatched (sonnet).
Task 6: fix re-review — Approved. Merged --no-ff as 3b98645 (87a799a + b36616e).
Task 6: complete
Gate at 3b98645 above. Task 7 (docs, §16, runbook, guides, README, exit check) dispatched (sonnet, in-tree) ∥ whole-branch final review (opus) on b08605212466f76e5931ef141f5f7afbeea55297..3b98645 — as in Phases 2–3, one fix wave covers both.
Phase 5 plan drafted (11 tasks, 4 waves; quarantine via StockLocSchema not LocKey; move kinds grn_accept/grn_reject; GRN nn = PO instalment count; PO-first document lock order with createPo the exception; receive/close-short in a grn module; POST /items in catalog; requestNewProduct in Phase 5; roles widened for createItem/answerProductRequest; one migration 0006). Pre-flight scan dispatched (opus) → .superpowers/sdd/2026-09-04-backend-phase-5-procurement/preflight-scan.md.
Final whole-branch review (opus) — With fixes. No Critical. I1 Orders.tsx ticketFor picks the oldest (withdrawn) ticket after a re-dispatch; I2 Shell sidebar badge counts a cancelled ticket forever (`!== "Received"`); I3 store Reports ledger counts a cancelled ticket as stock that left (Issued out + Opening overstated); I4 issue register counts withdrawn tickets. Minors: cancel-vs-handover race untested; Cancelled not tested as refused at handover/receive; H9 guard host-local (flake window); vacuous coffee-counter snapshot case; makeBatch does not fold a repeated recipe line (recipes are keyed — comment); MakeDistribute headline count; TicketDrawer reason hint promises a record nothing reads; negative made gets the wrong half of the sentence (verbatim store behaviour). Runbook: §8 "one ticket event" sentence now false; migration 0005 rollback hazard (Cancelled rows break a Phase 3 image's response validation) → Task 7 addendum sent. Phase 5 heads-ups (GRN id shape per PO; move kinds grn_accept/grn_reject; quarantine not in LocKey; builders; scope helpers; sequence-row serialisation) already anticipated by the Phase 5 plan — the scan verifies.
  Ruling: one fix wave (opus, worktree) for I1–I4 and minors 5–11; lock-order enumeration confirmed cycle-free; process note adopted — a widened status union means a grep over all of UI/src, not the task's file list.
Task 7: DONE — 11e25a7 (docs, §16, runbook incl. the rollback hazard, guides, README; exit check all lines pass except the browser check — port 3000 held by a stray process; kind → CI). Review dispatched (sonnet, docs-scoped).
Task 7: review — Needs fixes per the reviewer, but its Critical is a timeline misread: the §16 rows describe the final fix wave (aa34c9ddc06c7410f, still running — Orders.tsx ticketFor, Shell badge, Reports ledger/issue register), not b36616e (Task 6's fix round). Ruling: no Task 7 fix; the rows become true when the fix wave merges — its re-review must confirm those three sites. Minor: DEFAULT_SHELF_LIFE_HOURS listed under the domain guide's public surface though not re-exported → Phase 5 docs task. Task 7: complete (conditional on the fix wave).
Final fix wave — 658ecbc (13 files; I1–I4 each proven to fail when reverted; M5 cancel-vs-handover race proven; ui 354, api 368). Concern: Reports.tsx movers still sums withdrawn tickets into 'Issued from store' → fix round 2 dispatched (resume aa34c9ddc06c7410f) with a repo-wide grep for remaining ticket-status predicates; re-review covers both commits.
Fix round 2 — cb9f889 (movers → hasLeft; prod Dashboard 'Dispatches out today' KPI open-or-left; sweep of every s.tkt/fromStore read; ui 355). Scoped re-review of 658ecbc + cb9f889 dispatched (sonnet).
Final fix wave re-review — Approved (all eleven + round 2 addressed; one pre-existing flake seen once: writes.test.ts ~809 make-tile refusal toast under act()/setTimeout(0) load — Phase 5 hygiene item). Merged --no-ff as e6fa32c. Gate above. Phase 4 complete: ledger copied to docs, fast-forward into develop, workspace deleted. Follow-ups carried to Phase 5/6: writes.test flake; Phase 6 — a counter's door for cancelling shop tickets; ticket history surfaced; snapshot roster from payers; ua-spec rewrite; Playwright + load check.
