# SDD ledger — plan: docs/superpowers/plans/2026-09-04-backend-phase-3-movement-chain.md
Setup 2026-09-04T00:24:00Z: branch feat/phase-3-movement-chain cut from develop @ 8326384 (Phase 2 merged). Plan drafted by opus agent; Task 11 (pos credit ceiling + reservation-aware post-lock) being appended; pre-flight scan pending its final text.
Hotfix on develop 5b2f95d: deploy.yml literal ${{ }} in a comment broke the workflow file (red 0s Deploy run on every push since Phase 1); cache-to quoted in both workflows; actionlint clean. Ruling: committed directly to develop (CI config, independent of Phase 3) and ff'd into feat/phase-3 — cost if wrong: none, the branch had no commits.
Plan committed at 1650ced (11 tasks, 5 waves). Ruling: wave 1 (T1 contract ∥ T2 domain — disjoint packages) dispatched now while the pre-flight conflict scan runs in parallel (opus, reads the whole plan, writes the table to .superpowers/sdd/2026-09-04-backend-phase-3-movement-chain/preflight-scan.md) — the scan's findings bear on waves 2–4; a wave-1 finding costs one re-dispatch. BASE for T1/T2: 1650ced.
Hotfix on develop 1650ced: CI had been red since Phase 1's merge — trivy-action@0.28.0 unresolvable (tags are v-prefixed; now v0.36.0) and pnpm audit registry timeouts (now retried, warn-only on network failure, still fails on advisories). Nobody had checked CI after the Phase 1/2 pushes — Ruling: every push to develop is followed by a CI check within the session (gh run watch) from now on; the kind install runs in CI for the first time on this push — cost if wrong: none.
Ruling: the two CI hotfixes on develop were merged INTO feat/phase-3-movement-chain with a merge commit (f3be25f) — the branch had already diverged by the plan commit, so --ff-only was impossible; develop still receives the phase by fast-forward at the end — cost if wrong: one merge commit in history. Worktree agents merging --ff-only from the phase branch pick it up on their next dispatch; T1/T2 (based on 1650ced) merge fine.
Task 1: implementer DONE — a4b1d32 (worktree-agent-a4c1e577789d6b0cc); contract 27 tests. Review dispatched (sonnet).
Task 2: implementer DONE — 5a0f2f3 (worktree-agent-ac330e7a4e6f89bab); domain 34 tests. Review dispatched (sonnet).
Hotfix on develop: availability concurrency test was order-dependent (red in CI); now accepts either consistent interleaving + a forced two-transaction test of the PK-conflict path. Merged into feat/phase-3 (8e8848b). Watching CI run for develop.
Task 2: review — Approved (2 minors: planAppproval rounds appr where the store did not — deliberate, tested; Received→Closed entry unreachable, disclosed). Merged --no-ff as 252d11e; worktree removed.
Task 2: complete
Task 1: review — Approved (no issues; createRequest/cancelRequest access includes prod because the kitchen raises requests today — spec §9.2 terse, brief correct). Merged --no-ff as d6d6ba5; worktree removed.
Task 1: complete
Pre-flight scan (opus) → preflight-scan.md: 55 interface rows, 0 same-wave file conflicts, 6 Blocking / 10 Should-fix / 15 Notes. Rulings (plan amended by the planner before wave 2):
  B1 lockBalances: no SEP — sort cells by (loc, it) and lock in that order. B2/B3 sequence-dependent assertions become relative (capture the first id, expect +1). B4 module stubs are routes.ts-only empty plugins (as Phase 2) so knip stays green. B5 Task 8 owns every UI test that calls a now-async action — rewrite with the writes.test stub pattern or delete naming the server test; Task 6's commit body names server tests only. B6 the kitchen's ticket creators move too: new Task 12 (wave 3) `modules/production/` with POST /distributions and POST /prod-orders/:id/dispatch + PROD_ORDER_TRANSITIONS in domain/transitions.ts; Task 8 cuts distribute/dispatchOrder over as well → no dual-running tickets (spec §14 "nothing dual-runs" outranks the plan's seam) — cost if wrong: one extra module this phase instead of next.
  S7 builder ids from a range above the seed. S8 render.test.sh path. S9/S10/S11/S12/S16 → §16 rows in Task 10 (prod raises requests; onCommit superseded by in-transaction pg_notify; override history text = spec §8.3 "Handed over — supervisor override"; answerShopAsk grant > asked is a refusal, not a clamp; the post-lock handover sentence is NEW). S13 appr gets multipleOf(0.001) via Task 4's contract edit. S14 ids before balance locks everywhere (Tasks 5, 6, 7, 12). S15 Task 11 Files block lists common.ts. Notes: planner applies the cheap unambiguous ones and lists the rest.
Hotfix on develop: CI images job failed on CVE-2026-31789 (libssl3 in distroless nodejs24-debian12; 32-bit only) → .trivyignore with reason + exp:2026-09-30, trivyignores input on both scans. Merged into feat/phase-3 (cc47572). Next CI run is the first to reach the kind install.
Plan amended and committed at fa7c850 (12 tasks). Planner's ruling accepted: module stubs are all four files, wired like _template (routes.ts-only stubs would fail check-boundaries.sh); issueTicketRow split into allocateTicket + writeTicket so ids precede balance locks. Wave 2 dispatched: T3 SSE (opus) ∥ T4 movement primitives (opus), worktrees, BASE fa7c850.
CI on develop GREEN at 5138b14 (run 33823512722): check job, chart job, images job incl. Trivy scans and the kind 'helm install' step — the first fully green run since the backend landed.
Task 3: implementer DONE_WITH_CONCERNS — f6a4dce (worktree-agent-aa00ceff558c47131); api 199 tests, sse.test 8/8 ×3. Ruling: stream teardown from preClose (Fastify's own close handler runs before plugins' onClose) accepted — the brief's onClose hung under test; the extra stream-open/closed log lines and x-request-id header stay (hijack removes the stream from the onResponse log) — cost if wrong: two log lines. Reviewer to weigh: no listener-reconnect test. Review dispatched (opus).
Task 4: implementer DONE_WITH_CONCERNS — 65c43e1 (worktree agent-a66e3c21e9f45351b); api 213 tests. Concern accepted as a plan rule: two-transaction race tests must warmPool(t, n) first or pg's lazy connect serialises them (the brief's case was vacuous — now fails without the lock/sort). Carried into every wave-3 dispatch. Review dispatched (opus).
Task 4: review — Approved (7 minors). Merged --no-ff as ea35a06; worktree removed (slow: node_modules deletion is what times a 2-minute shell out). Minors: (1) given.request hard-codes the history author; (2) one suffix counter shared across the three builders; (3) TicketRefType hand-restated; (4) check-boundaries greps 'update reservations' case-insensitively → wave-3 briefs warned; (5) lockBalances creates a zero balance row (user-visible 'carries') → wave 3 locks only cells it moves; (6) stubs open a no-op transaction at boot — gone with wave 3; (7) assertTransition sentence odd for multi-word states → Task 10 §16 row. Ruling: (1)(2)(3) fixed in-tree before wave 3; gates run with --force (turbo replayed a stale green for a reviewer).
Task 4: complete
Task 3: review — Approved with I1 (a late end/error from a replaced LISTEN client starts a second listener and leaks the first → duplicate deliveries) + minors (shutdown assertion satisfied by the opening retry frame; notify payload parsed without Zod; writeHead drops CORS headers; app.sse decorator unused; Last-Event-ID resync → §16 row; nginx events location lacks X-Forwarded-* headers). Ruling: merge now so wave 3 can start; fix round 1 (I1, M1, M3, M7) runs in the T3 worktree in parallel and merges after a scoped re-review — cost if wrong: a follow-up merge.
Builders tidy in-tree d8513a0 (per-family counters, history author from p.by, TicketRefType from schema). Task 3 fix round 1 dispatched (resume aa00ceff558c47131, worktree kept). Wave 3 dispatched from BASE d8513a0: T5 requests (opus) ∥ T6 tickets (opus) ∥ T7 shopasks (sonnet) ∥ T11 pos (opus) ∥ T12 production (opus), worktrees.
Task 12: implementer DONE_WITH_CONCERNS — ff7f884 (worktree-agent-a1344771f2b2ac2e3); api 234, domain 37. Concerns: brief said 'neither uses assertTransition' (custom sentences) — Ruling: the guard must still read the table (canTransition) even with a custom sentence; reviewer checks. Brief's race case was vacuous (cover check refused first) — fixed by the implementer; other wave-3 reviewers told to check the same shape. Declined-order case guards on a seed row that does not exist → test should insert one. Review dispatched (sonnet).
Task 11: implementer DONE_WITH_CONCERNS — 97ebd6d (worktree-agent-a04c3342168d37de4); api 234, ui 323, domain 38. Rulings: race test uses a lock-holding transaction instead of the brief's unimplemented /transfers race — accepted (restore the service-vs-service version in the wave-3 integration sweep once Task 7 merges); staffCreditTaken needs a partial index on bills(payer_kind, payer_id, at) → integration sweep adds migration 0003 (drizzle-kit generate); refunds netting → future phase (§16 note via Task 10); Pos.tsx preview says "this session" while the server counts the month → Task 8 updates the wording (its screens). Review dispatched (opus).
Task 7: implementer DONE — 6801aa3 (worktree-agent-a1441e1c2d1069f24); shopasks 11 tests, api 234. Review dispatched (sonnet).
Task 5: implementer DONE — 296bb2f (worktree-agent-af89a02a431712a18); requests 28 tests, api 251. Deviations accepted: master name is "Milk 1L (toned)"; trimmed-before-rejected precedence follows the snippet + UI; hist assertions use toContainEqual because the seed stamps history at fixed times of day later than a morning test run (fixture artifact, not a code defect — Task 10 notes it); brief's issue-ticket race was vacuous → two genuine races added (other wave-3 reviewers already told to check the shape). Review dispatched (opus).
Task 12: review — Needs fixes: (I) transition guard hand-rolled, PROD_ORDER_TRANSITIONS decorative → canTransition as the boolean with the kitchen's sentences; (I) declined-order test inert (no Declined seed) → insert one in the test file + a parametrised positive for New/Accepted/In kitchen/Ready. Fix round 1 dispatched (resume a1344771f2b2ac2e3).
Task 11: review — Approved with I1 (tender × payer.kind not coupled — a staff-credit tender with a patient payer skips the ceiling) → fix round 1 dispatched (resume a04c3342168d37de4) with the inert timing assertion; minors: reserve() relies on callers to lock (→ in-tree: reserve() takes lockBalances itself); Pos.tsx re-implements the predicate (→ Task 8 uses breachesCredit); report nits.
In-tree 0ce9a0b: reserve() takes lockBalances itself (Task 11 review minor 2) — belt and braces for the lock-before-read rule.
Task 7: review — Approved with I (no true race test for the answer row lock) → fix round 1 (test only) dispatched, worktree kept; minor: decline checks the reason before the transition (brief's ordering). Merged --no-ff as 83aa28c.
Task 3: fix round 1 — 8e47713; the stale-client race could not be reproduced (pg destroys the socket on error, end lands within 1 ms) so the guards stand as defence in depth with a relay-based tripwire; a real shutdown hang on a black-holed listener socket found and bounded at 2 s. Ruling: keep the relay test, watch CI; if flaky, delete it rather than weaken the guards. Scoped re-review dispatched (sonnet).
Task 12: fix round 1 — 8a8afb0 (table-driven guard proven load-bearing; declined case with a local order builder; it.each positive). Note: given.prodOrder belongs in test/builders.ts once Task 11 lands (integration sweep). Scoped re-review dispatched (haiku).
Task 3: fix re-review — Approved (guards correct; relay test sound, 12/12 ×3; shutdown bounded; M1/M3/M7 addressed). Fix merged --no-ff as 974f2b6. Worktree removal deferred to a background step (node_modules deletion is slow).
Task 3: complete
Task 12: fix re-review — Approved. Merged --no-ff as f1b6e61; worktree removal deferred.
Task 12: complete
Task 5: review — Approved with I (issue-ticket does not fold a repeated item before the cover check — inherited from the brief and the store; two milk lines of 8 against 12 L both pass, 16 L reserved) → fix round 1 dispatched (resume af89a02a431712a18): fold before cover in issue, and in approve's freeFor; plus the never-exercised open-reservation term gets a cup case. Minors: two managers approving different requests read the same ftp (advisory; issue re-checks) → §16 note; setLineApprovals indexes by array position; every(qty>0) vs the UI's filter; history timestamps inconsistent. UI app.test 'manager at /stock' timed out once under load → integration sweep bumps the UI testTimeout. Merged --no-ff as ede3eb5; worktree kept for the fix round.
Task 9 (UI live updates client) dispatched early — depends only on Task 3 (merged); its files are disjoint from Task 8's. BASE ede3eb5. Opus, worktree.
In-tree 2775f5d: migration 0003 partial index for the staff-credit read (Task 11 concern); UI testTimeout 20 s (Task 5 review's load-induced flake).
Task 7: fix round 1 — dfa60d9 (real race test; failed without the lock, restored). Scoped re-review dispatched (haiku). Note: under parallel load snapshot.test's 150 ms budget and sse.test's shutdown case flaked once each for the T7 agent → integration sweep considers the timing budgets.
Task 6: implementer DONE — 9a4435d (worktree-agent-a380963d60598badf); tickets 20 tests, api 243; both row-lock races proven. Concerns: hist.at(-1) wall-clock dependence (seed times) → helper; transfer's lockBalances not race-provable (the tkt sequence row serialises minting) — kept, comment honest; repo-wide flakes under CPU contention (snapshot 150 ms budget, auth timeouts, UI renders) → integration sweep: make the snapshot budget tolerant of load and revisit api timeouts. Review dispatched (opus).
Task 11: fix round 1 — 6a43d1e (tender↔payer kind coupled with a sentence; staffCreditTaken also filters tender = 'Staff credit' so a cash bill in a staff member's name is not credit; both lock races time the sale itself, proven). Under load one run flaked (snapshot 150 ms budget; sse shutdown; the pos lock race's 400 ms hold) → integration sweep: widen the holds to 1000 ms; replace the snapshot wall-clock budget with a query-count assertion and move the latency SLO to Phase 6's load check. Scoped re-review dispatched (sonnet).
Task 7: fix re-review — Approved. Merged --no-ff as 2d50644.
Task 7: complete
Task 11: fix re-review — Approved. Merged --no-ff as b40f8a4 (97ebd6d + 6a43d1e).
Task 11: complete
Task 5: fix round 1 — 5c73de0: POST /requests refuses a repeated item ('Combine the <item> lines into one' — NEW sentence → Task 10 §16 row); issue folds approved lines before the cover check; cup case exercises the held term. Known gap noted for §16: a pre-rule request repeating an item is approved line-by-line (safe direction) and refused at issue. Scoped re-review dispatched (haiku).
Integration sweep (in-tree 3cba0a2): given.prodOrder in builders; pos holders 1000 ms; snapshot budget 500 ms (query shape, SLO → Phase 6); api testTimeout 30 s.
Task 5: fix re-review — Approved. Merged --no-ff as f15085b.
Task 5: complete
Task 9: implementer DONE — 9be6851 (worktree-agent-ac0b48e245e433723); ui 331 (9 new). Deviation accepted: the reconnect guard captures its own AbortController (the brief's guard read a nulled controller and reconnected behind a sign-out). Rulings: the server's opening `retry: 1000` means a flapping stream retries every second (EventSource semantics; a dead server still gets the ladder) — acceptable, §16 note via Task 10; online/visibilitychange handling → Phase 6 polish; narrow GETs for req/tkt/shopAsks are Task 8's. Review dispatched (sonnet).
Task 6: review — Approved (7 minors: linkedRequest row lock taken after the balance locks → hoist above postMoves so the order is documents → ids → balances; loadMaster pulls recipes no ticket rule needs → loadItems/loadLocations; from.n/to.n unguarded; a vacuous every() on releasedAt; HandoverBodySchema is a strictObject so the override needs an explicit {} body → Task 8's client sends {}; dead otp.trim(); emitChanged not literally last). Merged --no-ff as 6d1bc0c. Minors 1–4 → in-tree tidy alongside Task 8.
Task 6: complete
Gate after wave 3 (6d1bc0c, --force): contract 28, domain 41, ui 323, api 316; lint OK; chart renders. Task 8 (UI cutover, 14 actions) dispatched — opus, worktree, BASE 6d1bc0c. Tickets minors 1–4 dispatched as an in-tree tidy (sonnet).
Task 9: review — Approved (minors: redundant stop() in the dead-session branches; LF-only frame splitter matches the server; doc comment cites §6). Merged --no-ff as 4152eb1.
Task 9: complete
Tickets tidy in-tree 554b93b (lock order documents→ids→balances; loadItems/loadLocations; guarded location names; honest release assertion). Task 9 merged 4152eb1 before it.
Task 8: implementer DONE_WITH_CONCERNS — 7766b54 (worktree-agent-adbdba9d1b2375af4); ui 314, api 316; live check by curl + Playwright end to end. Concerns: prod/Tickets Receive control still on a literal status; OrderDrawer offers Dispatch from every open stage (table-driven — accept); screens reset forms before the server answers so a refusal lands on an empty form (Phase 2 pattern; reviewer to weigh — Ruling if Important: actions carrying a form return Promise<boolean> and screens reset on true); a zero-qty ask costs a round trip. Review dispatched (opus).
Task 8: review — Needs fixes: (I) screens reset forms/close drawers before the server answers (a refusal lands on an empty form; the Phase 2 pay pattern awaits + busy + clear-on-success) → fix round 1 dispatched (resume adbdba9d1b2375af4): form-carrying actions return Promise<boolean>, screens await + busy; minors folded in (literal status gates → selectors; OrderDrawer cover disable; fallback-sentence test). Merge dry-run vs HEAD clean.
Task 8: fix round 1 — dcff130 (nine form-carrying actions return Promise<boolean>; screens await + busy; status-literal gates → selectors; OrderDrawer cover disable; it.each fallback sentences; render cases proven). Note for Phase 4/6: makeProduct/setOrderStatus reset synchronously until they cut over; ticket buttons without forms have no busy lock. Scoped re-review dispatched (sonnet).
Task 8: fix re-review — Approved. Merged --no-ff as daf71a5 (7766b54 + dcff130).
Task 8: complete
Gate at daf71a5: see above. Task 10 dispatched (sonnet, in-tree) with task-10-notes.md.
Whole-branch final review dispatched (opus) on 5138b14e0e4281ac5c69648f6450d9b2727454bd..daf71a5 in parallel with Task 10 (docs land separately and get their own task review) — Ruling as in Phase 2: both read-only; one fix wave covers both.
Phase 4 plan drafting dispatched in parallel (opus) → docs/superpowers/plans/2026-09-04-backend-phase-4-production.md (uncommitted until Phase 3 lands on develop).
Final whole-branch review (opus) — With fixes. C1 payer identity unvalidated (a fresh payer.id restarts the ₹3,000 ceiling; payer_name written verbatim); I1 receive locks balances before the linked request row (the tidy hoisted handover only); I2 credit read takes no lock (two tills both pass); I3 POS preview counts every tender over 7 days while the server counts staff credit over the month; I4 no way to release a stranded reservation / cancel a ticket; I5 a short appr array zeroes lines with trimmed:false. Minors M1–M12 (vacuous events asserts; production location guards; distribute to kitchen allowed; route-level counter scoping on transfers; OTP shown to the sender; override audit unread; dead otp.trim; no busy lock on handover drawers; no per-user stream cap; requestFromStore sentence change; pord snapshot refetch; double retire).
  Rulings — fix wave now (opus, worktree): C1 → payers table (schema + migration 0004 + seed from fixtures; posRepo.payer; refuse unknown; write the roster name; snapshot roster stays fixture-fed until Phase 6 — §16); I1 hoist; I2 pg_advisory_xact_lock per payer + warmed race test; I3 preview filters tender and names its window; I5 length assert; M2, M3, M8, M9 (cap streams per user), M12, M1. Task 10 carries the seven runbook/§16 items + rows for the fix wave. Phase 4 plan gets: voidTicket + POST /tickets/:id/cancel + Cancelled ticket state (I4); PROD_ORDER_TRANSITIONS trap for the status endpoint (refuse Dispatched explicitly); GET /prod-orders narrow reader (M11); payers table as the roster for refunds. Phase 6: OTP withheld from the sender (M5); override audit surfaced (M6); M4/M7/M10 notes.
Task 10: agent stalled after its exit check (uncommitted docs, dev API left on :3000, stray screenshot in the repo root, a 'waiting for fork' message despite the no-subagents rule) → nudged to finish in one commit with the addendum; Phase 3 final fix wave running in a worktree (a6ab96188923263a0).
Task 10: DONE_WITH_CONCERNS — b245dd2; exit check lines 1–9, 11 PASS, 10 (two-browser) not verified (curl SSE capture stands), 12 kind → CI. Concerns: ua-spec.html is a pre-implementation pitch that contradicts the other two docs → Phase 6 docs pass; a manual adjustment move sits in the local dev DB (harmless). Review dispatched (sonnet, docs-scoped).
User directive (mid-Phase 3): root CLAUDE.md refreshed after every phase (already Task 10's job) + nested CLAUDE.md per package (apps/api, packages/contract, packages/domain, UI) → dispatched now (opus, in-tree, docs-only commit); Phase 4 planner told to add the nested guides to its docs task.
User directive: a proper repo README → root README.md rewritten as the monorepo front page by the same docs agent (second commit); Phase 4 planner told to keep root+nested CLAUDE.md and README current in every docs task.
Task 10: review — Approved (minors: five fix-wave §16 rows read as present tense — they become true when the fix wave merges; nginx X-Forwarded-* on /events undocumented; system-design note does not name GET /events). Task 10: complete.
Phase 4 plan drafted (7 tasks, 4 waves; no POST /prod-orders — nothing creates one today, parked in §16; ticket cancellation returns a request to its approved status; setStatus refuses Dispatched as target and source). Pre-flight scan dispatched (opus) → .superpowers/sdd/2026-09-04-backend-phase-4-production/preflight-scan.md. Plan stays uncommitted until Phase 3 lands on develop.
Final fix wave — 23d367f (22 files): payers table + 0004_payers + seed; per-payer advisory lock (proven: both 200 without it); receive hoist (proven: deadlock without it); appr length; POS preview honest; production guards; distribute refuses kitchen; busy locks on hand-over drawers; SSE per-user cap 429; single retire; real events asserts. Gate: contract 28, domain 41, ui 340, api 327. Scoped re-review dispatched (sonnet).
Nested guides f1b1478 (apps/api 168, contract 114, domain 92, UI 159 lines + root 'Nested guides' section) and README rewrite 2583a1a (204 lines) committed in-tree. Loose ends → small docs tidy (sonnet): UI/README.md stale Layout/Issues text; root CLAUDE.md Commands line for pnpm lint should name the boundary script.
Final fix wave re-review — Approved. Merged --no-ff as 14d79be.
Gate on the complete Phase 3 branch (14d79be, --force): contract 28, domain 41, ui 340, api 327; lint OK (oxlint 0 errors, knip clean, boundaries OK); chart renders. Remaining before the fast-forward: the UI README / lint-line docs tidy (sonnet, in-tree), then ledger copy → develop.
Docs tidy 3d596d6. Phase 3 complete at 3d596d6: ledger copied to docs, fast-forward into develop, workspace deleted. Follow-ups carried: Phase 4 plan (ticket cancellation, status endpoint refusing Dispatched, narrow pord/batch readers); Phase 6 (OTP withheld from the sender; override audit surfaced; online/visibilitychange; snapshot roster from the payers table; ua-spec.html rewrite).
