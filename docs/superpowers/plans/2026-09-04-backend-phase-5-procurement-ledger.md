# SDD ledger — plan: docs/superpowers/plans/2026-09-04-backend-phase-5-procurement.md
Setup: plan drafted during Phase 4's close-out (11 tasks, 4 waves; uncommitted until Phase 4 lands on develop); pre-flight scan running (opus). Attribution: commits end with only 'Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>'.
Pre-flight scan (opus): 41 rows, 11 tasks, 4 Blocking / 6 Should-fix / 12 Notes. Rulings: F1 Task 1 owns the one LOC assertion in procurement.test.ts; F2 wave-2 suites read GET /snapshot slices, not the same-wave GETs; F3 patch schemas declared explicitly (no inherited defaults); F4 receipt mfg/exp = IsoDate | "" with default ""; F5 the store keeps display dates (Phase 1 convention) and date inputs convert at the edge via fmt.ts helpers owned by Task 2; F6 contract rate distinguishable from item cost in-test; F7 PurchaseOrders.tsx in Task 10's Files; F8 selectors' poValue delegates to domain; F9 Task 4 Step 4 import contradiction fixed; F10 tests for every NEW sentence. Lessons carried: widened-union UI sweeps grep all of UI/src; docs task refreshes root + nested CLAUDE.md + README; single Co-Authored-By trailer. Planner amending; plan commits on feat/phase-5-procurement once Phase 4 lands on develop.
Plan amended (all F1–F10 and twelve Notes applied; 4529 lines; 28 §16 rows planned in Task 11). Awaiting Phase 4's fast-forward into develop before cutting feat/phase-5-procurement and committing.
Setup: Phase 4 fast-forwarded into develop at f13ab6c and pushed (CI run 33843776499 in progress). Branch feat/phase-5-procurement cut from develop; plan committed at a0234b4 (11 tasks, 4 waves). Journal last idx 5 → Task 3's migration is 0006. Wave 1 dispatched from BASE a0234b4: T1 contract (opus) ∥ T2 domain + fmt date helpers (opus) ∥ T3 api scaffolding + builders + migration (sonnet), worktrees. Hygiene carried from Phase 4: writes.test.ts make-tile refusal toast flaked once under load (act()/setTimeout(0)) → Task 10 makes it deterministic.
Task 2: implementer DONE — 6cf0dc6 (worktree-agent-a97bba8082da45bca); domain 100, ui 358. Notes: domain vitest has no TZ pin (tests TZ-independent — Task 11 or a hygiene line); prqStatus awaits Task 6's consumer (knip); selectors' poValue unrounded until wave 3. Review dispatched (sonnet).
Task 3: implementer DONE — eda94d3 (worktree-agent-a775fa0e31ba4d2b4); api 379; migration 0006_rate_contracts_live_uq. Note: builder defaults use VN-001 and fixture items — wave-2 contract scenarios pick non-colliding pairs (the live-uniqueness index). Review dispatched (sonnet).
Task 1: implementer DONE_WITH_CONCERNS — a6d9906 (worktree-agent-a5959a9dfd907e065); contract 55. Out-of-scope but forced: five apps/api files (seed.ts duplicate quarantine row; four tests) — the fixture's quarantine row collided with the seed's hand-written one; accepted (disjoint from Task 3's files — verified by dry-run merge). Guides' stale constants line → Task 11. Review dispatched (opus — 19 routes).
Task 2: review — Approved (minors: expired-batch sentence trims the batch code; poValue 2 dp vs the store's unrounded copy until wave 3). Merged --no-ff as b6c4bfd. CI check job for the Phase 4 push passed (15 min — slow, not failing).
Task 2: complete
Task 3: review — Approved (minor: given.contract's doc comment should mention the live-uniqueness index collision → wave-2 dispatches say so). Merged --no-ff as 64908fb.
Task 3: complete
CI on develop GREEN for the Phase 4 merge (run 33843776499: 18 min total; check job 15 min).
Hotfix on develop: the audit step took 13.5 min (slow registry × pnpm retries × 3 attempts) → each attempt under timeout 120, empty output counts as unreachable, 10 s pauses; actionlint clean; merged into feat/phase-5 (a0f4074).
Task 1: review — Approved (carry-forwards: Task 4 lifts TWO quarantine filters — readers/master.ts and readers/stock.ts's UI_LOCS; Task 11 corrects contract/CLAUDE.md's constants sentence and adds a §16 row for PO_APPROVAL_LIMIT's move; routes.test's first new case is misnamed — hygiene). Merged --no-ff as dba68dd.
Task 1: complete
Wave 1 merged; typecheck 4/4. Wave 2 dispatched from BASE dba68dd: T4 reads (sonnet) ∥ T5 requisitions (opus) ∥ T6 purchase orders (opus) ∥ T7 grn (opus) ∥ T8 vendors + contracts (sonnet) ∥ T9 items + product requests (sonnet), worktrees.
CI: the audit-cap push superseded (cancelled by concurrency); run 33845133015 for the timeout-handling push in progress. Wave 2 (Tasks 4–9) all dispatched from dba68dd.
Phase 6 plan drafting dispatched in parallel (opus) → docs/superpowers/plans/2026-09-04-backend-phase-6-ops-go-live.md (uncommitted until Phase 5 lands on develop).
Task 4: implementer DONE — 994c736 (worktree-agent-afcf80875db3e0527); api 389. Review dispatched (sonnet).
CI on develop GREEN for the audit hotfix (run 33845133015: 6m55s total; check job 2m56s — the cap works).
Task 5: implementer DONE — dcd8a74 (worktree-agent-a7916cc9b6d3eb1c1); requisitions 13 ×3, api 391; race proven against the lock, not the pool. Notes: decline checks the note before the transition (cosmetic); approve does not load the master (matches the store). Review dispatched (sonnet).
Task 4: review — Approved (minor: standalone reads re-query userNames — pre-existing pattern). Merged --no-ff as 46c4d71.
Task 4: complete
Task 8: implementer DONE — 5610b9c (worktree-agent-a9aaf024828a8e1c5); vendors+contracts 16 tests, api 393; both races proven against their unique indexes. Concern: vendor rename collision → raw 500 → fix dispatched before review (resume).
Task 5: review — Approved (no issues; idempotency replay covered generically). Merged --no-ff as 2d1aa85.
Task 5: complete
Task 9: implementer DONE — 2432060 (worktree-agent-af4e5a339e3bffe74); catalog +8, productreqs +7, api 391; slug advisory lock proven. Review dispatched (sonnet).
Task 8: fix — 6cb454e (vendor rename collision → 422). Review of 5610b9c + 6cb454e dispatched (sonnet).
Task 7: implementer DONE — 2edae01 (worktree-agent-a464b618098cf8607); grn 16 ×3, api 394; both races proven; rebuild equality after rejects. Deviations accepted: expiry cases use istDate (TZ=UTC vs IST today); quarantine asserted via stock_balances until Task 4 merges (it has). Check at merge: requisition lock order ascending in both grn and purchaseorders. Review dispatched (opus).
Task 6: implementer DONE — 95fd096 (worktree-agent-a2b8eb3207a3886de); purchaseorders 30 ×3, api 408; three races proven. Concerns: given.po({lines:[]}) impossible (builder — integration tidy adds a guard); a line patch with both qty and rate applies only qty (brief's two message forms — §16 note or fix in review); no history on line edits (Phase 6 audit); suite ~100 s. Review dispatched (opus).
In-tree 74d5ca5: given.po tolerates an empty lines array (Task 6 concern).
Task 9: review — Approved (minors: productReqs wire not via strip(); per-call userName). Merged --no-ff as 92a1aae.
Task 9: complete
Task 8: review — Approved (minors: redundant non-null assertion; GSTIN stored as typed (case) — Task 11 note; contract PATCH accepts an empty body as a no-op while vendors refuse — asymmetry noted for the UI/Phase 6). Merged --no-ff as 491b060.
Task 8: complete
Task 6: review — Approved with I1 (a line patch carrying both qty and rate dropped the rate; the store took the rate) → fix round dispatched (resume a2b8eb3207a3886de). Minors for the integration tidy: patch writes the row twice when vendor+eta; two lockRequisitions mechanisms (both ascending — no deadlock) and round3 asymmetry between purchaseorders and grn; writeLines resets received/rejected (all callers Draft-guarded); suite 58 s from per-case reseed. Cross-module check: approveRequisition locks the requisition head (Task 5 confirmed) — mutual exclusion with addOrdered holds.
Task 7: review — Approved (minors for the integration tidy: full-rejection message prints an empty accepted total; closeShort bypasses the transition table; po line index vs line_no coupling; two sort predicates for one lock order; GRN id collision when two POs share a three-char tail → §16/RUNBOOK line via Task 11). Merged --no-ff as 20b4871.
Task 7: complete
Task 6: fix — 93218e4 (both fields honoured in one rewrite; composed message); controller inspected the 23-line diff — approved. Merged --no-ff as 90cd0ae.
Task 6: complete
Wave 2 merged; gate above. Task 10 (UI cutover: buyer + store screens, 2 store slices, 7 narrow readers, 3 selectors, 13 screens, 4 test files, the sitem drawer) dispatched — opus, worktree, BASE 90cd0ae — with task-10-notes.md. Integration tidy of wave-2 minors dispatched in-tree (sonnet; api files only).
Phase 6 plan drafted (12 tasks, 5 waves; support module own-tickets; two server reports; e2e/ Playwright workspace; dependency-free load script; PrometheusRule; OTP withheld except at the ticket's to; deploy prepared, not performed). Pre-flight scan dispatched (opus) → .superpowers/sdd/2026-09-04-backend-phase-6-ops-go-live/preflight-scan.md. Wave-2 integration tidy dispatched in-tree (sonnet; api module files only).
Correction: the Phase 6 pre-flight scan and the wave-2 integration tidy were actually dispatched now (the previous line was written ahead of the Agent calls). Task 11 controller notes written → task-11-notes.md (§16 items from the wave-2 reviews, hygiene, exit-check shape); Task 10 specifics to be appended at its merge.
Wave-2 integration tidy landed in-tree: 0a3e8e5 (lib/claims.ts lockRequisitions+addOrdered; 0-accepted message; closeShort via PO_TRANSITIONS; line_no carried; single PO patch write; contracts empty-patch refusal; GSTIN upper-cased; productReqs strip()). api 478. Left: per-case partial reseed for the purchase-orders suite (needs a db/seed.ts helper + test/db.ts harness — Phase 6 hygiene). Task 10 forked from 90cd0ae touches UI/** only; merge on top of 0a3e8e5 expected clean.
Phase 6 pre-flight scan done: 46 pairs / 9 conflicts / 11 gaps / 22 reality mismatches / 8 rubric; rulings F1–F27. Top three: PAR_FACTOR is a Record<LocKey, number> at HEAD (plan made it a scalar); Task 7 PrometheusRule uses a non-existent rch.fullname helper and a cert assertion that fails on its own FILL; employee numbers swapped (manager RC-3120, prod RC-1902, counter2 RC-4482) and support.test.ts never seeds. Planner resumed to apply F1–F27 + two Phase 5 hygiene carries (partial reseed helper; GRN tail widening).
Phase 6 plan amended: 4,297 lines; all F1–F27 applied (F22's TZ pins moved to new Task 13); R1–R19, G1–G12, RC1–RC8 applied; new Task 13 (Hygiene: seedDocuments/resetDocuments partial reseed; grnId widening to GRN-<yy><po no>-<nn>; TZ pins) in wave 2 → 13 tasks, 5 waves. Still uncommitted until the branch is cut from develop after Phase 5 lands.
Task 10: implementer DONE_WITH_CONCERNS — 06005c1 (worktree-agent-a0d6946a95bbca5ef); 26 files +1204 −1220; ui 348+1 todo, api 473 (tree predates the tidy); live-checked by curl and browser. Concerns: PurchaseOrders.tsx keeps four status comparisons as list buckets (not controls — accepted); addContract's parameter carries a vendor name the body drops (tidy later); GET /events sends no CORS headers (same-origin today — Phase 6 note); a cleared ETA cannot be unset (schema optional not nullable — §16 note). Review dispatched (opus).
Task 10: review — Spec Approved; Quality Changes required: B1 PoDrawer line blur writes unedited values; B2 ETA input writes per intermediate date and outside busy; B3 all-zero draft reaches the generic 400; B4 module-level IT snapshots miss a product the new drawer adds (store Requisitions/Contracts + four buyer screens). Minors: stale Stock.tsx comment; fromInputDate unused in production; OFFLINE table gaps; browser-only MRP-below-cost gate and MTO+opening-qty in the new-product drawers (no server counterpart — Phase 6); addContract parameter; tautological canSendPo/canCancelPo in the Draft branch; sendPo no longer closes the drawer; catalogVersion not bumped by applySnapshot (Phase 6). Fix round 1 dispatched (resume a0d6946a95bbca5ef): B1–B4 + comment + addContract narrowing + sendPo close + OFFLINE rows.
Task 10: fix round 1 — e07cbf5 (B1–B4 + minors; ui 356+1 todo; both guards proven failing without them). Scoped re-review dispatched (sonnet).
Task 10: re-review — All addressed (each guard proven failing without it; no new findings). Merged --no-ff as 28ea378.
Task 10: complete
Gate on 28ea378 running in background; whole-branch review dispatched (opus, range merge-base develop..28ea378); Task 11 dispatches when the gate is green.
Gate on 28ea378 GREEN: typecheck+test 8/8 (contract 4 files, domain 17, ui 10, api 43), lint 4/4 + knip + boundaries. Task 10 worktree removed. Task 11 dispatched in-tree (sonnet) from BASE 28ea378 with task-11-notes.md; whole-branch review running in parallel.
Whole-branch review — no Blocking. Important: (1) close-short accepts an Ordered order (canTransition Ordered→Received) — must be Partially received only, with a test; (2) contracts repo.update has no isUniqueViolation catch → raced reactivation 500s (retry reads the refusal); (3) UI contractRate/contractFor ignore the validity window the server's activeContractRates enforces. Minors recorded for Phase 6: recv=0+rejected>0 line silently skipped; grn repo duplicates purchaseorders' head/sources/wire/userName and strip() ×4; moq/reorder admit negatives; catalog's NEW cost sentence; zero-qty requisition line gets the empty-draft sentence; fromInputDate alias; stale contracts.test comment; fixes.test NPR pin weakened; claims.ts double sort; multipleOf vs step=any → generic 400; productreqs forLoc unconstrained for a counter; closing a closed contract says closed. Verdict: ff after the three Important land + Task 11. Fix wave dispatched (sonnet, worktree from 28ea378) — Task 11 is in-tree, so the fix lands in a worktree and merges after Task 11.
Fix wave: deccc9b (worktree-agent-a680454d35eb6b27f) — close-short Partially received only; contracts update catches rate_contracts_live_uq (name lookup moved before the update: Postgres aborts the tx on the violation); contractInWindow shared predicate (domain +3 tests) used by contractRate/contractFor. api 480, domain 103, ui 359+1 todo. F2 race proven failing without the catch. Scoped re-review dispatched (sonnet).
Task 11: implementer DONE — ad7c1a4 (in-tree); 40 §16 rows; exit check §14 row 5 PASS (7/7 migrate, seed, full curl walk, rebuild-balances byte-equal, gate 8/8, lint, helm, build-site, SSE live check in browser). Corrections vs notes: refetch DOES have an items narrow reader (applyItems); five endpoints move ordered_qty. Awaiting the fix-wave re-review, then merge deccc9b on top.
Fix wave: re-review — All addressed (F2 rollback path verified in drizzle's session; contractInWindow pure, exported, knip clean). Merged --no-ff as d52f823; docs follow-up eaf3112 (runbook paragraph, two §16 rows → 42, domain guide). Final gate on eaf3112 running in background; fix-wave worktree removal running.
Final gate on eaf3112 GREEN: contract 55, domain 103, ui 359+1 todo, api 480; lint (oxlint + knip + boundaries) OK; helm:test OK; build-site OK. Fix-wave worktree removed.
Phase 5 complete: ledger copied to docs/superpowers/plans/2026-09-04-backend-phase-5-procurement-ledger.md; fast-forwarding into develop.
