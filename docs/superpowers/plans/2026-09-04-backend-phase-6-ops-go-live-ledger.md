# SDD ledger — plan: docs/superpowers/plans/2026-09-04-backend-phase-6-ops-go-live.md
Setup: plan amended after pre-flight (13 tasks, 5 waves; scan at preflight-scan.md, all F1–F27 applied). Briefs 1–13 generated. Branch not yet cut — waits for Phase 5's fast-forward into develop.
Ruling (wave 1 sequencing): dispatch Task 1 (contract) ∥ Task 2 (domain) from the branch head; Task 3 (api) only after Task 1 merges — Task 3 writes `hist: []` into a schema-derived type that does not have the field until Task 1 lands, so a Task 3 forked earlier cannot pass its own gate. Cost if wrong: one wave-1 slot idle for Task 1's duration (~20 min).
Carried from Phase 5 Task 10 review (Phase 6 hygiene, Task 13 or 10): fromInputDate unused; browser-only MRP-below-cost/MTO hints in the new-product drawers; catalogVersion not bumped by applySnapshot/resync; module-level IT snapshots in counter/prod Requests screens; GET /events has no CORS headers (same-origin only); PatchPoBodySchema.eta optional-not-nullable so an ETA cannot be unset.
Carried from the Phase 5 whole-branch review (Phase 6 Task 13 hygiene unless a task already owns the file): receipt line recv=0 & rejected>0 silently skipped (refuse); grn repo duplicates purchaseorders' head/sources/wire/userName; strip() copy-pasted in four repos; moq/reorder admit negatives; catalog 'Cost must be more than zero' vs the browser's sentence; zero-qty requisition line answered with the empty-draft sentence; fromInputDate alias of fromWireDate; fixes.test NPR pin no longer exercises routing; claims.ts sorts twice; multipleOf(0.001/0.01) vs step=any inputs → generic 400; productreqs forLoc not constrained to a counter's own location; closing a closed contract answers 'closed'.
Setup: Phase 5 fast-forwarded into develop (dc6ae84) and pushed. Branch feat/phase-6-ops-go-live cut; plan committed at 9b5b765. Journal last entry must read 0006_rate_contracts_live_uq — Phase 6 writes no migration. Wave 1 dispatched from BASE 9b5b765: T1 contract (opus) ∥ T2 domain (opus), worktrees; T3 after T1 merges (ruling above).
Task 2: implementer DONE — e7ed49f (worktree-agent-af5adb9d62fb193ad); domain 113 (+10, each seen failing first); knip clean (tests import from index). Notes: SHOP_ASK_TRANSITIONS Sent→Asked is a general edge with no reachable consumer until Task 4's door (guide line for Task 11/12); ledgerRow before/inWindow boundary needs Task 6's repo test to pin at<from vs at>=from. Review dispatched (sonnet).
CI on develop GREEN for the Phase 5 merge (run 33855509245, head dc6ae84: check, helm chart, container images all success).
Task 2: review — Approved (minors for Task 11: domain guide's guarded-edge paragraph gains SHOP_ASK Sent→Asked; 'What lives here' rows for support.ts/reports.ts). Merged --no-ff as 456c2d6.
Task 2: complete
Task 1: implementer DONE_WITH_CONCERNS — 107b5d4 (worktree-agent-a7b22414ca1eb0aca); contract 61 (+4, red first). Concerns: fixtures/seed.ts gained hist on seedTkt (outside the Files list — needed by documents.test's parse; accepted, Task 3 told); repo typecheck reds = apps/api lib/tickets.ts:57,86, snapshot/readers/documents.ts:43, snapshot/service.ts:32 (roster), UI __tests__/screens.test.tsx:229,230 only — no UI production file; turbo hides the second package's reds. Review dispatched (opus — 5 routes, schemas, constants move).
Task 1: review — Approved both (accept seed.ts hist row). Cross-task: F1 Issued/handover/receive history rows are Task 4's trail (writeTicket writes none today) → wave-2-notes; F2 rating narrowing → task-3-notes; F5 OUTLET_COLS duplication in snapshot scope/readers → Task 3 deletes both; F3 mutable ALL_LOCS/OUTLETS (leave); F4 cancel access proof → Task 4 auth cases. Merged --no-ff as f9513d9.
Task 1: complete
Task 3 dispatched (sonnet, worktree) from BASE f9513d9 with task-3-notes.md.
Task 3: implementer DONE_WITH_CONCERNS — 29640ed (worktree-agent-a001b68b38af05d4c); api 487 (486 pass; the one red is tickets.test's counter-cancel access case, red since Task 1's widening, Task 4's per its brief line 137 — accepted). Touched lib/tickets.ts, snapshot/service.ts, snapshot/scope.ts + three tests beyond the Files list (all anticipated by the notes). reports/service.ts stub uses 'void db; void reportsRepo;' to keep the brief's Record<string, never> type — reviewer to judge. Review dispatched (sonnet).
Task 3: review — Approved both (six extra files accepted as the notes' minimal fixes; reports stub idiom accepted; one red test = Task 4's). Merged --no-ff as 314d8b0.
Task 3: complete
Wave 1 merged at 314d8b0 (typecheck green everywhere but UI screens.test.tsx:229,230 — Task 10's; api 486/487 with the counter-cancel case Task 4's). Wave 2 dispatched from BASE 314d8b0: T4 tickets+snapshot (opus) ∥ T5 support writes (opus) ∥ T6 reports (opus) ∥ T7 chart (sonnet) ∥ T8 e2e Playwright (opus) ∥ T9 load check (sonnet) ∥ T13 hygiene (sonnet), worktrees, each with wave-2-notes.md.
Wave 2: all seven dispatched (T4 a825a73d…, T5 a2ebb16d…, T6 ae98839e…, T7 a5a58b2a…, T8 a9b59174…, T9 afe7bde2…, T13 a3f9a38a…); T8 uses PORT 3180/5180 + db rch_e2e, T9 PORT 3190 + db rch_load, so live runs do not collide.
Task 9: implementer DONE — ce92c85 (worktree-agent-afe7bde2c09ca8e8f); loadcheck.mjs + package.json script. Local baseline under load-avg ~19 (six other agents): snapshot p95 601/1984 ms, bills p95 2018/3151 ms at c=10/30 — FAIL vs §12 150/200 ms; not the verdict (Task 12 re-measures alone). Setup gaps for Task 12: RATE_LIMIT_PER_MINUTE must be raised for a load run; a bill loop drains one SKU (top up or sell an MTO item). Review dispatched (sonnet).
Task 5: implementer DONE_WITH_CONCERNS — 6f75497 (worktree-agent-a2ebb16d76b7cd1d1); support.test 18 (+15 incl. a race), api 501/502 (Task 4's red). Ruling: the race pair changed from Resolved-vs-Closed (a legal edge — passes by scheduling luck) to two Resolved taps, proven failing without for update — accepted, better test. Three NEW sentences not four (setTicketStatus success line is ops.ts:110 verbatim) → Task 11. No by_user index (plan-accepted). Review dispatched (opus).
Task 7: implementer DONE_WITH_CONCERNS — 19bccf5 (worktree-agent-a5a58b2a78898fc95); helm:test, helm lint, production render with FILLs, actionlint all green. Found: render.test.sh's 11 negated assertions were no-ops under set -e (! never trips it) → refute helper; hook-annotation check narrowed to 'helm.sh/hook:'; div→divf (Sprig integer div); resources: moved above probes in api-deployment so the sed-range assertion can see it; values-staging.yaml untouched. Review dispatched (sonnet).
Task 6: implementer DONE — f1e8561 (worktree-agent-ae98839e334f9bca8); contract 66 (+5), reports.test 15, api 501/502 (Task 4's red). Deviations argued: pos passes the sale's own at into creditTakenThisMonth (IST month boundary); reportsRepo.carriedAt so an M12 zero row with no window move still lists; payUntilRefused replaced by sellPastTheCeiling (cover check fires before credit). EXPLAIN on 55 rows is a Seq Scan (index matches; enable_seqscan=off plans quoted for Task 12). Pos.tsx:62 comment names posRepo.staffCreditTaken → Task 10. Review dispatched (opus).
Task 9: review — Spec Approved; Quality Changes required: hammer() lacks try/catch (a connection failure kills the run); no --help (an unknown flag runs a live load); pickSellable message; max=undefined on an all-refused run. Fix round 1 dispatched (resume afe7bde2c09ca8e8f); controller inspects the diff.
Task 9: fix — c1c3f0f (try/catch in hammer; --help before login; message; max=n/a; header note); controller inspected the diff — approved. Merged --no-ff as c4f5c81. (A cd into the worktree had persisted in the shell for one command — the first merge attempt ran there as a no-op; redone from the root. Rule: absolute paths, never cd.)
Task 9: complete
Task 13: implementer DONE — 98a804f (worktree-agent-a3f9a38a982c6730e); domain 115, contract 61, api 487/488 (Task 4's red); purchaseorders suite 10.4 s → 5.6 s (noisy host), shuffle-order green. Judgement: vendor seeding moved from seedMaster into seedProcurement (resetDocuments truncates vendors — FK failure otherwise). domain vitest already had the TZ pin (Phase 5) — only contract's added. Review dispatched (sonnet).
Task 5: review — Approved both. Important (Task 10, not Task 5): Support.tsx:227's Reopen button now calls a refused transition (only support moves a ticket to With support) — drop it, a reply reopens via statusAfterReply. Minors: no 'tickets' narrow reader in refetch (Task 10 adds one over GET /support/tickets); ownership 404 contradicts the plan's Global Constraints' 403 rule → Task 11 §16 row (tickets cancel returns 403 for the same shape); reply-on-closed rule inline in service:76 → mayReply in domain support.ts (Task 10, in-tree, when it previews it); users.name read twice per raise/reply; message ordering ties lexicographic above m9; ownership pinned on one of three :id routes. Merged --no-ff as 8403114.
Task 5: complete
Task 4: implementer DONE_WITH_CONCERNS — ca928e3 (worktree-agent-a825a73d4ce59df65); api 502/502 (the counter-cancel red is green now); +6 snapshot, +9 tickets cases. Concerns: the cancel race is pinned by the ticket row's for update (linkedShopAsk's lock cannot be shown to matter — one ask holds one live ticket) — docstring and test say so; seeded TKT-0440 still ships hist: [] (db/seed.ts is Task 13's file — one-line loop over seedTkt's history, in-tree after Task 13 merges); 'Handed over' is a NEW trail sentence → §16; given.ticket now writes its Issued row (builders.ts) and lib/tickets.test.ts gained one assertion; snapshot.test.ts now reseeds per case (slowest file; resetDocuments candidate). Review dispatched (opus).
Task 7: review — Approved both (gate re-run by the reviewer: render.test, helm lint ×2, helm template ×3, actionlint). Minors: hook regex could take the quoted-key form (grep -qE 'helm\.sh/hook"?:'); values-staging.yaml has no ingress.certificateArn FILL though RUNBOOK's first-time setup says both files need it (pre-existing) → Task 12's release-prep list. Merged --no-ff as 726a11a.
Task 7: complete
Task 6: review — Approved both (three deviations judged correct: at into creditTakenThisMonth preserves the IST month; carriedAt keys only, no double count; sellPastTheCeiling pins the same fact). Minors for the in-tree wave-2 tidy: reports.test:137 cannot fail (assert from/to identities), tautological room/closing assertions → literals, sellPastTheCeiling asserts the refusal text; service.ts:31-35 three reads not one snapshot (comment). §16 rows (Task 11): stockLedger has no per-location narrowing; creditReport lets any counter read any payer's month; partial index means patient/dept credit is a seq scan answering 0; Task 12 re-runs EXPLAIN with a bound literal. Merged --no-ff as d1a7278.
Task 6: complete
Task 13: review — Approved both (27-table reset verified against the schema; vendor move net-neutral for db:seed; old GRN ids intact). Minor for the tidy: ids.test.ts:37-38 collision assertions → literals. Merged --no-ff as f8aeaec.
Task 13: complete
Task 4: review — Changes required. BLOCKING: the OTP still travels to the sending location through five write responses (writeTicket/readTicket return the live otp; issue-ticket, shop-ask answer, dispatch, distribute, transfer results carry it — the issuer can hand over without the recorded override). Important: the seed's missing ticket-history replay loop (Task 13's file, now merged) breaks two of Task 4's own assertions when added. Fix round 1 dispatched (resume a825a73d4ce59df65; merges the phase branch first for Task 13's seed shape; owns requests.test:265 and production.test:55 for this round). Minors: nameOf re-read; two support queries not one; builders' Issued row on collected tickets; no backfill for live tickets' Issued row (runbook + §16).
In-tree wave-2 tidy dispatched (sonnet): reports.test window/literal/message assertions, reports/service three-reads comment, ids.test literals — files disjoint from Task 4's fix round.
Wave-2 tidy landed in-tree: 2fe942d (reports.test window/literal/message assertions; reports/service comment; ids.test literals); reports 15, domain 115.
Task 4: fix round 1 — 4a10d5d (on merge b7acf59 of the phase branch): write responses carry otp ''; readTicket redacts unless Issued (branch unreachable today, commented); seed replays the ticket trail; the four pinned assertions corrected; documents.test strips nothing now; snapshot.test on resetDocuments except the roster case. api 534. Scoped re-review dispatched (sonnet).
Task 4: re-review — All addressed (every write response carries otp ''; handover checks the DB row; seeded trail exact-pinned twice). Minor: snapshot.test's roster case deactivates a payer and nothing restores it (order-dependent) → tidy list. Merged --no-ff as ef6b90d.
Task 4: complete
Wave 2: Tasks 4, 5, 6, 7, 9, 13 merged (head ef6b90d); Task 8 (e2e) still running — its files (e2e/, ci.yml, install-test.sh, lockfile) are disjoint from Task 10's, so Task 10 (UI cutover) dispatched now from BASE ef6b90d (opus, worktree) with task-10-notes.md; Task 8 merges when reviewed. Gate on ef6b90d running in background.
Gate on ef6b90d: contract 66, domain 115; ui 358/360 with ONE failure — writes.test 'leaves the quantity on the make tile when the kitchen is short' — a load flake (3/3 green standalone; load average 15 with two agents running stacks); api run cut short by turbo. UI typecheck red at screens.test.tsx:229,230 remains Task 10's. Flake → tidy list (make settleUntil robust under load).
Task 8: implementer DONE_WITH_CONCERNS — f4cb70a (worktree-agent-a9b59174ad9091c25); e2e/ 6 specs / 12 tests green twice (35 s, 39 s); turbo test never reaches a browser. Concerns: OTP-redaction assertion behind E2E_OTP_REDACTED=1 (Task 4 landed since; Task 10's UI half pending) — the fix wave turns it on and deletes the switch; e2e stack needs SEED_FORCE_PASSWORD_CHANGE=false + both login rate limits raised (set by install-test.sh when E2E=1); local run used a throwaway static+proxy server (vite.config pins :3000). Findings for others: labelOf names a shared route key after the wrong role; GRN toast / on-order warning print raw 3-dp quantities; DataTable row-action button also opens the drawer (no stopPropagation); MAX_STREAMS_PER_USER=8 reached by browser tests. Review dispatched (opus).
Task 8: review — Spec Approved with conditions; Quality Changes required. B1: findings 2/3 (3-dp quantities) were WRONG — unitTotal/fq are the shared rule; withdrawn from Task 10 (message sent). B2: support scoping assertion vacuous at BASE (support browser-local) → gate behind E2E_SUPPORT_SERVER like the OTP. I1 .otp-v misses two bare-span sites → assert the value; I2 signin toast pins the label Task 10 changes → anchor on the tail; I4 kitchen asserts a toast not the tile figures; I5 contexts never closed (stream cap); M1 isVisible never waits; M2 loose '3'; M3 item selects lack aria-labels (→ Task 10; specs switch to getByLabel in the fix wave); M4 install-test comment untrue under E2E=1; M5 .env.example login limits (→ Task 12); M6 the claim not asserted. Fix round 1 dispatched (resume a9b59174ad9091c25).
Task 8: fix round 1 — b4ef626 (all nine items; I1 proven both ways; M4 via BASE_ARGS so the helm upgrade leg installs the chart defaults; M6 asserts the milk row gone while the list is live). Two runs 35.6 s / 34.9 s. Scoped re-review dispatched (sonnet).
Task 8: re-review — All addressed (helm template verified both legs). Minor: request-chain.spec closes its three contexts only on the happy path → fix wave (try/finally). Merged --no-ff as a7768de.
Task 8: complete
Wave 2 complete at a7768de. Task 10 (UI cutover) still running from BASE ef6b90d; its merge is the wave-3 close.
Whole-branch review split in two: the server/contract/domain/chart/CI/e2e half dispatched now (opus, range merge-base develop..a7768de); the UI half + integration seams after Task 10 merges. Task 11 dispatches after Task 10 merges; one fix wave covers both halves + tidy-notes.md.
Task 10: implementer DONE_WITH_CONCERNS — 31eaeb7 (worktree-agent-a652cfc7e1921f19c); 38 files +779 −352; gate 8/8 green (contract 66, domain 116, ui 370+1 todo, api 534), lint/knip/boundaries OK. Rulings: (1) ledger foot uses unitTotal per column, not ledgerTotals (mixed units — accepted; §16 row; ledgerTotals keeps its test caller); (2) new 'Sent from this counter' card on /#/tickets so the counter's cancel door is reachable (accepted; smoke note); (3) four screens outside the list moved module-level IT/LOC snapshots into render (accepted — the registries are empty at import now); (4) labelOf reads the role's NAV first; a counter at /#/orders still reads 'Orders' (no entry) — accepted, recorded; (5) E2E_OTP_REDACTED can be switched on; 'Close ticket' is a new rule-derived button. Review dispatched (opus).
Whole-branch review (server half) — BLOCKING 1: the OTP is derivable from the ticket id (makeOtp(n) is a published pure function of the same n as TKT-0<n>, exported from @rch/domain and bundled into the browser) — redaction on the wire is not redaction in fact. Ruling: mint with crypto.randomInt(100000, 1000000) in allocateTicket/writeTicket; remove makeOtp from domain's public surface (keep nothing deterministic); shape assertions in lib/tickets.test.ts:25 and builders.ts:57; §16 row rewriting the old 'OTP in the clear' acceptance. Important: (2) UI/src/data/master.ts still imports @rch/contract/fixtures via the re-export kept for it — repoint at @rch/contract and delete the three re-export lines (after Task 10); (3) PAR_FACTOR is UI-only tuning in the wire package → domain; (4) reports.test:74-90 picks a row with no orderBy under literal assertions → orderBy itemKey; (5) deploy.yml's verify step unbounded (no job timeout-minutes, Docker Hub pull, curl without --max-time) → timeout-minutes + kubectl wait + --max-time. Minors recorded: readTicket's Issued branch has no location check (always return ''); support writes wake every browser (per-user list — fine once the narrow reader lands); loadcheck --password in argv + NaN on a missing flag value; builders SUP band padding mismatch + builders.test slice(-3); roster served whole to store/prod/buyer (§16 wording). Verdict: ready for the exit check once 1 is settled and 2–5 land in the fix wave.
Task 10: review — Spec Approved (unitTotal deviation and the Sent-from-this-counter card accepted; ledgerTotals to be deleted); Quality Changes required: I1 prod/Tickets renders the guarded OTP in the outbound table (kitchen cannot read an inbound code — real bug); I2 counter TicketDrawer's withheld-OTP sentence wrong on inbound tickets past Issued; I3 'Confirm receipt' offered on an outbound ticket (server 403). Minors: delete ledgerTotals; procurement.ts comment; Reports.tsx failure state; wire.ts ALL_LOC from the schema; Tickets.tsx dead empty prop; Section/Feed + one dotFor helper. Recorded: no counter-side handover for a transfer (§16/follow-up); labelOf 'Orders'. Merge-time: flip E2E_OTP_REDACTED=1 and E2E_SUPPORT_SERVER=1 in ci.yml (fix wave deletes the switches). Fix round 1 dispatched (resume a652cfc7e1921f19c).
Task 10: fix round 1 — 9dd1de8 (I1–I3 + six minors; ticketDot helper; settleUntil now throws instead of falling through; ui 379+1 todo, domain 115; turbo 8/8, knip quiet). Scoped re-review dispatched (sonnet).
Task 10: re-review — All addressed. Minor: the two new Cancelled sub-branches (counter drawer, prod inbound cell) lack a direct test → fix wave. Merged --no-ff as 36463b7.
Task 10: complete
Wave 3 complete at 36463b7. Dispatching Task 11 (docs, in-tree, sonnet) ∥ the UI-half/integration whole-branch review (opus).
Fix wave dispatched early (opus, worktree from 36463b7, agent a7ecf2f9…): tidy-notes.md in full — random OTP (blocking), master.ts fixtures import, PAR_FACTOR → domain, reports.test orderBy, deploy.yml timeouts/kubectl wait, minors, e2e switches deleted + getByLabel + counter tickets selectors; smoke twice on 3160/5160 + db rch_fix. The UI-half review's findings go to the same agent as an addendum (second commit) so the wave stays one dispatch + one scoped re-review. Task 11 (docs) running in-tree (sonnet).
Whole-branch review (UI half + seams) — BLOCKING B1: loadSnapshot's catch sets auth 'ready' with empty registries (seed gone) → every screen throws into the ErrorBoundary on a degraded API → auth 'failed' state + Retry. Important: I2 the kitchen has no OTP input (every kitchen handover is the labelled override; new copy says the opposite) → the store's field pattern on the kitchen's outbound row; I3 manager ItemsStock has no Cancelled branch (reachable now the counter can cancel) → widened-union sweep; I4 redactOtps keys on location only — the manager at 'rest' receives OTPs on the wire → role test; I5 e2e docstrings/comments stale + scope the Product getByLabel. Minors: Pos credit 'Checking…' for ever on a failed read; Reports retry does nothing; ledger rows filtered by the browser catalogue (§16); trail Feed copied ×3 → TicketTrail; no counter cancel wire case; screens.test:82-88 renders nothing; kitchen has no ticket drawer/trail (§16); bare HH:MM on old support tickets (§16). Verdict: one Blocking fix from exit-ready. Addendum sent to the fix-wave agent (second commit).
Fix wave: a8f762b (server-half list + e2e switches deleted) + 19d486a (UI-half addendum) on worktree-agent-a7ecf2f95b3ed2d1a; gate 9/9 (contract 66, domain 115, ui 387+1 todo, api 535), lint/knip/boundaries, helm, actionlint, build-site; smoke 12/12 twice (33.1 s, 32.0 s) with the OTP and support assertions unconditional. New ptkt drawer closes M6. Left: guide/§16 prose (Task 11 told), nameOf re-read (deliberate), plan doc's loadcheck --password quotes. Scoped re-review dispatched (opus — the diff spans domain/api/UI/e2e/deploy).
Task 11: implementer DONE — 9898a50 (12 files +1143 −878); 46 §16 rows (7 marked '(fix wave)' for re-verification); UA table 24 rows with test names; two user-flows SVGs relabelled not redrawn; ua-spec/system-design 'draft/for client review' headers untouched (Task 12 may tidy). Review dispatched (sonnet) after the fix wave merges so the reviewer reads the final tree.
Fix wave: re-review — All addressed (OTP random+stored as char(6); redactOtps role test both readers; auth 'failed' swept; ptkt drawer; Cancelled sweep complete; constants moved; deploy bounded; switches gone; tests added). Three minors → post-merge mini-tidy: Sign out beside Retry on the failed page; /^\d{6}$/ at lib/tickets.test.ts:26; stale e2e comment at request-chain.spec.ts:64-67. Merged --no-ff as 7765b7f. Full gate on 7765b7f running in background; Task 11 review (a451e246…) running against 9898a50 + the fix-wave tree.
Post-wave mini-tidy dispatched (sonnet, worktree from 7765b7f): Sign out on the failed page; OTP shape assertion; stale e2e comment. Gate on 7765b7f running in background (turbo, lint, helm, build-site, actionlint). Next: merge the mini-tidy → docs fix round if the Task 11 review asks → Task 12 (exit check, in-tree, opus) with task-12-notes.md.
Gate on 7765b7f: contract 66, domain 115, ui 387+1 todo GREEN; the api suite failed at the FILE level in every file — Docker Desktop died mid-run (Postgres 'shutting down' / 'in recovery mode'; 'Docker Desktop is unable to start'; disk 74% used, 4.3 GiB free). Environmental, not code. Docker restarted; recovery + builder/image prune + db:up running in background; the api suite re-runs when Postgres is back. The mini-tidy's api test step will have hit the same outage.
Task 11: review — Spec Changes required + Quality Changes required: 7 Blocking guide/runbook statements false of HEAD (deleted seed/ops named; old GRN format ×2; 'write response not redacted'; ledgerTotals row; domain table broken by the otp.ts paragraph; runbook readyz path; makeOtp), 14 Important (system-design §08/§12 still pitch-tense; alert counts; RDS settings missing; 'all five constants'; four missing §16 rows; stale fix-wave markers; loadcheck --password; masthead 'draft/for client review' → 'As built'), 8 Minor. Findings written to task-11-review.md; fix round 1 dispatched (resume a5be5714507c851a0).
Docker back (~5 min); build cache prune reclaimed ~12 GB (disk 45% used now); rch-postgres healthy on 5439. Re-running the api suite on 7765b7f in background.
Gate on 7765b7f fully GREEN after the DB came back: api 535/535 (45 files); contract 66, domain 115, ui 387+1 todo; lint, helm, build-site, actionlint OK.
Mini-tidy: aac2460 — controller inspected the 30-line diff (Sign out navigates to /login after logout; shape assertion; comment) — approved; merged --no-ff as f072dab; lib/tickets + session tests re-run green in-tree after the DB recovery.
Task 11: fix round 1 — 82280a1 (all 29 items; §16 now 53 rows). Docs re-review dispatched (sonnet, read-only) ∥ Task 12 (exit check, in-tree, opus) dispatched from 82280a1 with task-12-notes.md; any leftover docs finding goes to Task 12 as an addendum.
Task 11: re-review — All addressed (all 29; the seven Blocking re-verified against the code; the sweep for makeOtp/seed.ts/ledgerTotals/old GRN/'Phases 1–5'/'Lands in'/'For client review' finds nothing live).
Task 11: complete
Task 12: the exit check, walked in-tree on 82280a1. Gate from a clean tree, all green and none of it cached: `pnpm turbo typecheck test --force` 9/9 (contract 66, domain 115, ui 387+1 todo, api 535 in 45 files, 1m26s), `pnpm lint` exit 0 (oxlint api/e2e/domain/contract 0 warnings; ui 265 warnings 0 errors; knip silent; boundaries OK), `pnpm helm:test` exit 0, `build-site.sh` exit 0 (18 docs, 3 app files), `actionlint` exit 0. Migrations 7/7, no pending. The support chain walked end to end against `pnpm dev`: SUP-0044 raised and invisible to the buyer ("There is no support ticket SUP-0044."), all four rules verbatim, idempotent replay identical (SUP-0045 twice, one row); REQ-2026-0913 → TKT-0441 with the OTP only on the collector's read, wrong OTP refused, handover moved store 12→8 and coffee 0→4 and released the hold; ASK-063 granted as TKT-0442 then cancelled, the ask back to Asked and the reservation gone; both reports, the credit report agreeing with the refusal to the rupee (taken 2000, room 1000); roster 4/4/3 = the payers table; rebuild-balances 0 mismatches over 53 rows. Browser walk: prod home /#/orders, the ptkt drawer's OTP field + labelled override (wrong code refused, right one handed over, history Issued > Handed over), the manager's transfers table showing "Cancelled / Withdrawn — nothing moved", and the failed-snapshot page with Retry and Sign out reached for real (snapshot 429 while the session held) and recovered by Retry. Smoke twice, 12/12 both: 40.2 s and 38.5 s, reseeded between. Restore drill rehearsed end to end (dump 101 KB, restore, rebuild 54 rows, empty diff, 2.9 s), scratch db dropped. SIGTERM: dev exit 0 in 0.088 s, production-mode exit 0 in 5.095 s with /readyz 503 for 66 probes and the stream closing on a second `retry: 1000`. Config miss exits 2 naming the variable. Headers/CORS/413/429 all as specified (320 requests → 298×200 + 22×429). Forced 500 carries a reference id matching two log lines and no SQL. All 24 UA rows verified against the last green run: 24 of 24, none unverifiable. `kind` is not installed here, so the cluster smoke is CI's evidence, not this walk's — recorded as such.
Task 12: two findings, neither patched (this task verifies, it does not fix). **Fix wave 2 (1)** `GET /snapshot` misses §12's 150 ms p95 at concurrency 30 on this machine — c=10 PASS (104.5 ms / 141.6 ms, load average 2.72), c=30 FAIL (2860.3 ms / 262.2 ms, load average 2.56), snapshot-only c=30 p95 4429.7 ms with `pg_pool_idle 0` and `pg_pool_waiting` peaking at 771 against `max: 10`; the snapshot's `Promise.all` fans out to 24 readers, ~13 transactions a request. §12 states the target for the staging instance, so it is re-measured there; the pool size is the thing to revisit. **Fix wave 2 (2)** `plugins/health.ts`'s bare `catch` discards the readiness check's own reason, so a schema at 0/7 reads only "Not ready: database." — diagnosability, not correctness. Docs corrections made inline: the runbook said six migrations as of Phase 4 (seven as of Phase 6). Also noted, not changed: `values-staging.yaml` still has no `ingress.certificateArn` key — written into the release-prep table rather than patched, because this task changes no behaviour.
Task 12: complete — one commit on 82280a1, "Walk the production checklist and prepare the first deploy". §12 carries a `#### Verified — Phase 6 (2026-09-04)` sub-section with every line's observed evidence; RUNBOOK §§6, 12 and 13 carry the drill, the load numbers and the two smoke times; §11 opens with "The release, prepared and not performed". **Nothing in this build pushes `staging` or `production`** — the five FILLs, the secrets and the three fast-forward merges are written down for the account owner and stop there. Phase 6 complete.
Task 12: DONE_WITH_CONCERNS — e11668f: §12 walked (all PASS except: per-suite rebuild narrowed to seed + walk db; performance FAIL at c=30 — /snapshot p95 2860 ms, bills 262 ms; PASS at c=10 — 104.5 / 141.6 ms, load avg 2.7); smoke 12/12 ×2 (40.2 s, 38.5 s); restore drill 2.9 s empty diff; release-prep tables written; ledger copied. Fix wave 2: (1) /snapshot pool queuing — 24 readers × ~13 transactions/request against max 10 → run the snapshot's readers on ONE connection/transaction (and make the pool size an env knob), re-measure alone; (2) health.ts bare catch drops the readiness reason; (3) values-staging.yaml certificateArn FILL. Dispatched (opus, worktree from e11668f) — the last code change before the ff.
Fix wave 2: db5c24e (worktree-agent-a4f886d68e5254eb6) — snapshot and standalone reads on one connection (pool acquire 40 → 1 per snapshot, pinned by test; pg_pool_waiting peak 771 → 20 at c=30); DB_POOL_MAX knob (default 10 unchanged); readiness reason carried; staging certificateArn FILL; §16 rows. Re-measured alone: c=10 snapshot 102.7 / bills 126.1 ms PASS (load 3.6); c=30 snapshot 1548 ms (was 2860) / bills 248 ms (was 262) — still FAIL vs 150/200 on a laptop hosting Postgres; recorded as the staging measurement's baseline. api 541, ui 387+1 todo. Scoped re-review dispatched (sonnet).
Fix wave 2: re-review — All addressed (lock safety proven: read-only tx, no locks reachable, release on error verified live; acquire-count test exact and falsifiable; behaviour preserved across 158 tests; DB_POOL_MAX validated + rendered; readiness reason without leaks; staging FILL in production's shape). Three minors → second commit (resume a4f886d68e5254eb6): GET /recipes onto withReadTransaction + test; expectedMigrationCount() inside the try; §16 staging row line 41→42.
Fix wave 2: follow-up 273a2d0 (recipes on withReadTransaction + test; journal read inside the try; §16 line fix) — controller inspected the diff — approved. Merged --no-ff as a7152d2. api 542.
Fix wave 2: complete
Phase 6 complete at a7152d2: final gate running; then ledger copy refreshed, fast-forward into develop, push, CI.
